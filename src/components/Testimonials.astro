---
const testimonials = [
  { quote: `Serviço impecável e atendimento personalizado.
Finalmente encontrei uma assessoria que entende as necessidades da minha pequena empresa.`, author: 'Joao Victor' },
];
---
<section class="testimonials-section">
  <div class="container">
    <h2 class="section-title">O que dizem sobre nós</h2>

    <div class="add-testimonial-link">
      <a href="/deixar-depoimento">Deixe seu depoimento também</a>
    </div>

    <div class="slider-container">
      {testimonials.map(testimonial => (
        <div class="testimonial-card">
          <p class="quote">"{testimonial.quote}"</p>
          <p class="author">- {testimonial.author}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .testimonials-section {
    background-color: #3E646F;
    color: white;
    padding: 5rem 0;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: 2.25rem;
    text-align: center;
    margin-bottom: 1rem; /* Diminuído para dar espaço ao novo link */
  }

  /* ESTILOS PARA O NOVO LINK */
  .add-testimonial-link {
    text-align: center;
    margin-bottom: 3rem;
  }
  .add-testimonial-link a {
    font-family: 'Lato', sans-serif;
    color: #a8dadc;
    text-decoration: underline;
    font-style: italic;
    transition: color 0.3s ease;
  }
  .add-testimonial-link a:hover {
    color: #FFFAEE;
  }

  .slider-container {
    display: flex;
    gap: 1.5rem;
    overflow-x: auto;
    padding-bottom: 1.5rem;
    scrollbar-width: none;
  }
  .slider-container::-webkit-scrollbar {
    display: none;
  }
  .testimonial-card {
    flex: 0 0 80%;
    padding: 2rem;
    border: 1px solid rgba(197, 169, 138, 0.2);
    border-radius: 0.5rem;
    background-color: rgba(29, 53, 87, 0.5);
  }
  .quote {
    font-family: 'Lato', sans-serif;
    line-height: 1.7;
    margin-bottom: 1rem;
    font-style: italic;
  }
  .author {
    font-family: 'Montserrat', sans-serif;
    font-weight: 600;
    color: #a8dadc;
  }

  @media (min-width: 768px) {
    .slider-container {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      overflow-x: visible;
    }
  }
</style>